package apps.pixel.the.key.activites.Home;

import java.util.List;

import apps.pixel.the.key.models.home.sliderResponse;

public interface HomeSliderInterface {
    void getSliders(List<sliderResponse> restaurants);
}
